# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: >-
          pip install -r requirements.txt && gcloud auth
          activate-service-account
          devfest2023-rsjk@nice-mechanism-378917.iam.gserviceaccount.com
          --key-file=gcloud_account_key.json && chmod +x rsjk.sh && chmod +x
          imageToJson.sh && chmod +x runCode.sh && python server.py
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEVFEST2023_D9DD0 }}'
          projectId: devfest2023-d9dd0
